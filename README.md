# Krono-Corp

Ademas del archivo login.kt se ha agregado un archivo llamado build.gradle.kts, ese archivo contiene lo necesario para correr la aplicacion con el comando .\gradlew :app:run --console=plain.
